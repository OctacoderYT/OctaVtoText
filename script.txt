
-------------------------------------------------------------------------------------------------------
						intro
-------------------------------------------------------------------------------------------------------


Hello guys! 
Welcome back to OctaCoder.

So without further ado, let’s dive in and get this app online! 🚀

(Channel trailer plays for 5-7 seconds)


--------------------------------------------------------------------------------------------------------					Description
--------------------------------------------------------------------------------------------------------

so lets get started.

first please create a new react project so i already created lets give heading Voice to Text lets run npm start great we have heading.

so lets start builing app now so what we need? we need a textarea then button and for select language english hindi marathi options right so lets create that first.
for option we use select tag then options we need 3 english hindi and marathi save it perfect we got options now below we need textarea lets give rows 8 and a button ok

now before we connect the microphone, we need access to the browser's built-in Speech Recognition engine every browser provides its own engine so for the browser to start listening to our voice we first need to create a speechRecognition reference so const speechRecognition then we use window method dot speechRecgonition or window.webkitSpeechRecognition coz chrome still uses webkitSpeechRecognition this avoids runtime crashes ok. 

now you might be wondering why are we using window? coz window is the global object of the browser and all borwser API's live inside the window object if you open browser console tab and type window you will see hundreds of built-in methods. 
since speech recognition is a browser API we access it using window.speechrecognition so this is the method we are just using it ok

now we need a place to store text that comes from the microphone so for that in react we do that using state so at the top lets import usestate hook to store changing data.

next create state for text

so const text current transcript when use spoke ok then setText function to update transcript and we'll use empty useState

now bind this to textarea value = text ok

now we have the speech recognition but how do we actually use the microphone?

we need to create an instance of speecrecognition right so we use useRef to store the speech recognition instance.
so lets import useRef first, useRef lets us store values that persist across renders
without causing re-renders.

now create recognition ref inside app ummm lets write comments here to not get confuse this is states 
now REFs 
now here we write const recognitionRef = useRef value null ok this ref will hold our speech recognition instance.

“Now we have a place to store the microphone instance,
but when should we create it?”

Now comes the MOST IMPORTANT PART of the whole app.

next step we use useEFFECT to initialize speech recognition so first import useEffect on top ok 

then lets also write comment here for useEffect ok 

now why useEffect?
We want to create the speech recognition instance only once,
when the app loads.
So we use useEffect with an empty dependency array.
This useEffect runs only once when the app mounts.

now Before creating the speech recognition instance,
we first check if the browser actually supports it.

if not we show a friendly alert "speech recognition not supported in this browser"
and stop execution, This prevents runtime crashes and unexpected errors.

alright now that we know speech recognition is supported how do we actually create the microphone engine?
now we create the new speech recognition instance - const recognition = new SpeechRecognition();
Think of it as turning on the microphone engine provided by the browser.”
“At this point, the microphone is created but not listening yet.

now Stores the SpeechRecognition object inside our useRef so it can be accessed later.

now By default, speech recognition stops after a single sentence.
But we want continuous voice to text, right?”
so recognition dot continuous = true;
This tells the browser to keep listening instead of stopping after one phrase.
also we can add recognition dot interimResults = true; this allows us show words while user is still speaking.

so Now that the microphone is listening continuously,
how does it know which language to recognize?

so for that we set the language using recognition.lang = "en-IN"; this tells the speech recognition engine which language to listen here we are setting it to english india for now later we'll make this dynamic so users can switch between languages

allright You now handle the onresult event properly

so recognition dot onresult = event 

Every time we speak,
the browser sends us speech data through an event object.

but this event contains multiple results some are temporary and some are confirmed words.

so lets create two variables.

let finalText = empty value; to store final text and 
let interimText = empty 
interim to store temporary words ok

Right now you created containers (finalText and interimText),
but they are still empty.

“The speech recognition event does not give us just one sentence.
It gives us an array of results.”

So we loop through event.results to read every spoken word.

for loop

let i = event.resultIndex where new results start reading.
i less than event.results.length loop until the last available result, then i++ move to the next speech result.

now inside this loop we access each speech result
so const res = event.results[i] array i each item inside event.results is a speechRecognitionResult object.

res contains the spoken text and whether that text is final or still in progress.

now we need to extract the actual spoken words
Right now res is a SpeechRecognitionResult object.
But we don’t want the whole object — we want the text the user actually spoke.

so transcript = res[0].transcript; 

“Each speech result can contain multiple alternatives,
but the most accurate one is always at index 0.”

“So res[0].transcript gives us the actual spoken sentence
as a normal string.

Right now we have the spoken text stored in transcript,
but we still don’t know whether the browser is still thinking or fully confident so here we'll use if else

if res.isFinal then finalText += means appends transcript + we'll give space so words dont run together ok.
if you dont do this words will repeat text will flicker.  

Whenever we get final text, add it to our textarea
and whenever we get interim text, show it temporarily

We already have: states ok So now inside onresult, after the loop, we update state.

if finalText should permanently go into the textarea.”
setText updates a state prev => arrow function prev + finalText previous saved text append then setInterim('') clears temporary speech else finalText does not exists so setnterim(interimText);

so now is the time to start microphone it should be started only after user interaction that means button click.

so now we will create a start/stop button. so we already have button here so now we need to know whether the microphone is currently listening or not?
so we create state

const listening, setListening = useState and the value is false ok this state will control button, prevent double start.

now instead of seperate start and stop function, we'll create one smart toggle.  

so lets declare function called toggleListening. it doesn't take any arguments, it relies on state and refs instead.

const recognition = grab the current value from a react ref called recognitionRef, for safety check if the speech recognition object doesn't exist yet, bail out early so nothing crashes.

now we'll check react state listening.
if you are not currently listening start the speech recognition. and update react state to reflect that the app is now listening,
else if you are already listening stop the recognition and update state to false and clears any interim, transcript text.
This function acts like an on/off switch:
now we just need to add this to a button so here onClick toggleListening function
here we'll use turnery operator if listening is true we stop else false then start.

This is the moment where our app actually comes alive.”

“Until now, we were just preparing logic.
Now we’re connecting it to real user interaction.”

“Let’s save the file and see what happens…”

“Oh — we got an error.”

setInterim is not defined.

“And that’s actually correct.”

“We’re calling setInterim, but we never created that state.”

“So for now, since we’re only testing basic microphone functionality,
I’m going to comment out all the setInterim lines.”

“This is how real development works.
You test in small steps.”

“Let’s save again…”

“And… perfect. No errors.”

“That means our microphone engine is successfully initialized.”

Alright… this is the moment of truth.”

“If I click the Start Mic button, the browser should ask for microphone permission.”

“Let’s click Start…”

“You’ll see a permission popup — click Allow.”

“Now the microphone is active.”

“Let’s test it…”

hello guys... oh wow its working.. whatever i am speaking is instantly getting transcribed inside the textarea.

that means our speech recognition engine is successfully connected and this is just the beginning.

if this already helped you understand browser API's better please make sure to subscribe to the channel. right now tap on that subscribe button, look at it this is soo cool guys every single word to word is getting transcribed in real time.

so now lets test if i click stop it should stop listening right so click stop and yes now its not listening perfect.

“That means our Start and Stop logic is working correctly.”

Now comes the interesting part…”

how do we make these language buttons work?”

“When I click Hindi, it should transcribe in Hindi.”

“When I click Marathi, it should switch instantly.”

“So now we’re moving from basic functionality… to professional-level features.”

so now we will create handleLanguageChange function first lets write comment bellow toggleListening function ok

comment language switch so our function starts here ok 

Before writing handleLanguageChange, we first need state to store the selected language.
so const language, setLanguage useState and by default we set it to en-in india make sure its string ok.
now we also need to change in recognition Instead of hardcoding English,
we now connect recognition to our language state.

then we create handlLanguageChange function with parameter newLang.

First, we update our React state. Whatever language user selected — English, Hindi, Marathi — we store it inside language state.

Now we need to access our SpeechRecognition instance. so const recognition = recognitionRef.current; Because earlier we stored our SpeechRecognition object inside recognitionRef, we can now access it anytime using recognitionRef.current.

and if its not aavailable we simply return and stop execution; 
This prevents errors and keeps our app safe from crashing.”

Now we temporarily store the new language inside pendingLangRef.
Before we save any pending language, we need a place to store it.”

“So just like we created recognitionRef, we now create another ref.
so here in ref const pendingLangRef = useRef(null) value null
This ref will temporarily store a new language value.

Now we also need to track microphone state internally using a ref.”
“Because state updates are asynchronous, we cannot fully trust the listening state inside async callbacks.”

“So we create another ref.”

so const isListeningRef = useRef(false) value false ok

Then inside toggleListening update it so here isListeningRef.current value is true ok and in else value false This keeps a real-time value of mic status that doesn’t depend on React re-render timing.

now back to our handleLanguageChange function.

We store the new language inside pendingLangRef.

so pendingLangRef.current = newLang; we do this because we cannot change language while recognition is running so we store it temporarily and apply it safely.

allright

now we check is the microphone currently active?
if yes, we stop recognition first because the web speech API does not allow changing language while listening.

else microphone is already off we dont need to wait, we directly update the recognition lagnuage, and then we clear the pending language.

so right now our handleLanguageChange function exists but the user cannot trigger it yet so now we connect our function to the select dropdown here in select tag we add the value and bind it to our language state then onChange event fires e.target.value gives us the selected language code and we pass that value to handlLanguageChange.

Now inside the <option> tags, we assign real language codes so value  en-IN means english india
2 option for hindi and then marathi, you can add as many languages as you want based on your audience ok.

lastly we handle the onend event so lets do that... inside useEffect, after onResult

so recognition.onend = () => {} This event runs automatically whenever speech recognition stops ok. 
then if pendingLangRef.current remember earlier when the user changes language while the mic is running we cannot change the language instantly while recongition is active so we stored the new language temporarily inside pendinglangRef.

when recongition fully stops, onend runs and this is safe to update the language. then clear the pending value, 

now we restart the mic automatically if it was already active. 
if isListeningRef.current recognition.start.

and lastly we handle errors 
recognition.onerror event we log the error speech recognition error e.error if mic denied or network errors this helps debugging its developer things 

finally we return a cleanup function inside useEffect coz when the component unmount we stop the microphone. otherwise it might stay active in the background so to stop the mic properly.

and thats it! finally our core functionality is complete now lets test it and see if everything works perfectly so save the file and we see a small warning thats okay we can ignore it so lets open the browser.

click start mic allow microphone access.

hello developers.
ohh nice its transcibing perfectly..
Whatever I’m speaking is instantly getting converted into text inside the textarea.
This is amazing...

Now let’s test Hindi.
Change language to Hindi…

Namaste dosto, bahot badhiya! Sab kuch ab Hindi mein record ho raha hai. Dosto subscribe kar do yaar!”
Perfect. Hindi transcription is working.
Now let’s test Marathi.

Aani aata mi Marathi madhe bolat aahe… khupach chhaan mitrano subscribe kara ki.

lets switch back to english.

I love it guys this is such a cool app.

now you dont need to manually write your notes.
just open this app and speak your your thoughts and everything gets written automatically this is super useful for the content creators, developers, and anyone who wants quick notes.

allright but lets be honest the functionality is powerful but the ui not that impressive yet so now we're going to transform this simple app into a Pro level UI and here's the interesting part 

we're not going to write 200 lines of CSS manually now ill show you how to use AI tools like chatGpt smartly to design beautiful UI faster.


=== === ==== ===== === === === === == CHATGPT DeSing SCript == == == ==== === ==== ==== === === == ===


so lets open chatGPT  

now ill show you very easy way to use chatGPT to design our app. now i'll give a simple prompt like - act as a senior software engineer + UI/UX expert and you hvae all the knowledge so i need you help to design my app ill give code ok and enter.

now chatGPT understood what i want so he gave us all information to desing app he asking for our code or screenshot of the current ui and what i want to improve ok so he wrote all details no need to read all the details. 

so now what ill do ill copy our function code from app.js so copy all the code and paste here and tell chatGPT this is the react app code that i build so i want you to design this app like pro level ui ok + add dark mode light mode theme. and most important dont change the functional code just write down css and make design attractive ok and hit enter.

now he writting the necessory css code this is what we want dark mode light mode theme clean and smooth layout responsive. you only need to replace your app.css so he wrote all the necessory css code look at this code If we had written this CSS manually, it could easily take 1 hour. its hard right but thanks to chatGPT we can do that 1hr work within a seconds.

so ill copy this whole css code click on this and it will copy all the code now go back to vs code
go to app.css file select existing CSS and replace it with the ChatGPT code.

save the file no issues now lets open app on browser. 
oh wow so chatGPT created a clean layout its nice simple desing but he forgot the dark mode light mode theme its ok so lets test start mic asking for mic permission allow it.

hello this is so cool. i love it. hi. hello. welcome to octacode. design is pretty good.
but its just basic design right also AI didnt add dark mode light mode theme so we need that lets go back to vs code and comment out all the code.

now lets give new prompt to chatGPT

design is ok ok not that pro also i want toggle button for dark mode light mode so add that and write whole code again and hit enter.

so now he is writing whole js code again code is same what we wrote he just adding toggle button nothing else ok so he wrote js and css code now lets copy js code and go back to ous vs code

now you'll understand why i commented all code coz i dont want to lose my original code so i commented out all code now lets add comment chatGpt design code.

paste the copied code here now do the same thing for css copy css code and paste it in css file save both files and lets see output.

oh wow nice now he added dark mode light mode great this is pretty cool desing not bad lets click start mic.

hi wow i like this code now and this animation button is also good which means you are speaking i like it heartbeat animation mode ok thats cool thats cool right let me check if this goes out of the box or is gonna stop there? oh ok we got scrollbar here ummm good basic design lets try another so ill give prompt like,

pretty cool desing but not that attractive can you make it more attractive also toggle button make it small like slider button for dark mode light mode ok also make little large textarea box and start/stop mic button make it more attractive now write whole code again dont forget to write please ok hit enter

the more you give instruction to ai the more AI plays with you so you have to be specific what you need its not that hard to write prompts ok.

now you know what to do copy the code paste it in relevant files save it hope this time design looks good lets see now.

its same design only toggle button changes that is what we wanted smaller button so thats good lets try some more.

also chatGPT gave suggessions like chatGPT voice mode with live waveform animation, so ill just say ok design it.

now again chatGPT writing all the code now again you know what to do copy the code and paste it save the files.

allright so we got little color change theme oh nice chatGPT added some animation but why this is wobbling screen i dont like that much desing. 

so ill try some more design ill play with chatGPT little more until i'll feel desing looks good till then ill do same thing copy paste the code.

so chatGPT gave me some these kind of desing which i didnt like that much but after trying more designs finally i like one of so this is the final desing which i like. 

It looks clean, modern, and much better than our basic layout.”

“You can also see that while I’m speaking, the words are appearing here in real time.”

Remember earlier in the code we commented out setInterim?

Now I simply uncommented those lines, and added a small div below the textarea to display the interim text — which shows temporary speech while the user is still talking.

This makes the transcription feel live and responsive.

Another small improvement is that you can also edit the text manually if you want.

after experimenting with ChatGPT and refining the prompts
This is the design I liked the most.

You can also try creating different designs using ChatGPT or any other AI tools.”

“Using AI for UI design is actually very simple — you just need to give clear instructions.”

“The more specific your prompt is, the better the results you’ll get.”

“So finally, we’ve completed the coding and the design of our app.”

“What’s the last step remaining?”

“Deployment.”

“Now we just need to deploy this app so anyone on the internet can use it.”

“And for that, we’ll deploy it on Vercel.”

“So let’s do that right now.”

















outro:

so thats it thank you for watching and learning from my videos and if you haven't subscribed yet please make sure to hit that subscribe button, like share and let us know your thoughts in the comments thanks for your support,  happy coding and see you in the next one.

====================================================================================================

chatGPT link - 
https://chatgpt.com/c/694adf6f-50c4-8323-94d9-44c891c156e9

